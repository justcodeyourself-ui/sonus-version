# sonus-version

Public version distribution and release metadata for **Sonus Music Streaming**.

### Configuration Schema (`version.json`)
The Sonus client app queries `https://raw.githubusercontent.com/justcodeyourself-ui/sonus-version/main/version.json` on launch to check for compulsory and optional updates without requiring a dedicated VPS.
